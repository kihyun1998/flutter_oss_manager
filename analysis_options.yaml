include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # This package is a CLI: `print` to stdout/stderr is its output mechanism,
    # not a debugging leftover. Disable the lint rather than sprinkle ignores.
    avoid_print: false

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
